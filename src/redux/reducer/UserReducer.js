import {createSlice} from '@reduxjs/toolkit';

const initialState = {
 
};

const UserSlice = createSlice({
  name: 'User',
  initialState,
  reducers: {

  },
});

export const {

} = UserSlice.actions;

export default UserSlice.reducer;
